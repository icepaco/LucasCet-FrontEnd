#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../binwrap/bin/binwrap-prepare" "$@"
  ret=$?
else 
  node  "$basedir/../binwrap/bin/binwrap-prepare" "$@"
  ret=$?
fi
exit $ret
