#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../elm-format/bin/elm-format" "$@"
  ret=$?
else 
  node  "$basedir/../elm-format/bin/elm-format" "$@"
  ret=$?
fi
exit $ret
